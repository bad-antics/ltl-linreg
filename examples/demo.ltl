// examples/demo.ltl — runnable demo for ltl_linreg

import std::io
use ltl_linreg as lib

fn main() {
    io::println("ltl_linreg demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}